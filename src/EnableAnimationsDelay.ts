// This file isnt required, but it's kept
// here for the sake of showing animations

export const ANIMATION_DELAY_MS = 2000;

export const testingDelay = (ms = ANIMATION_DELAY_MS) => new Promise((res) => setTimeout(res, ms));
