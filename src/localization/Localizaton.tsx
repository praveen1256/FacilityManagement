import LocalizedStrings from "react-native-localization";

import { en } from "./en";
export const locationzedStrings = new LocalizedStrings({ en });

// Run the following function only on development mode
// This function will check if the keys in en.tsx and ja.tsx are the same
